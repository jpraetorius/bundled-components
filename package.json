{
  "name": "bundled-components",
  "version": "1.0.0",
  "description": "Test for creating bundles with different bundlers",
  "main": "index.js",
  "scripts": {
    "serve": "donny",
    "build-faucet": "",
    "build-vite": "",
    "build-esbuild": "",
    "build-rollup": "",
    "build": "build-faucet && build-vite && build-esbuild && build-rollup",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "INNOQ",
  "license": "BSD-3-Clause",
  "dependencies": {
    "stimulus": "^3.1.1"
  },
  "devDependencies": {
    "donny": "^0.3.0",
    "esbuild": "^0.15.12",
    "faucet-pipeline-core": "^1.7.0",
    "faucet-pipeline-css": "^0.2.1",
    "faucet-pipeline-js": "^2.1.8",
    "rollup": "^2.79.1",
    "vite": "^3.2.2"
  }
}
